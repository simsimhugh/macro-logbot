# --- Secrets (절대 commit 금지) ---
.env
.env.*
!.env.example
*.key
*.pem
*.token
*.crt
credentials*
secrets/
auth/

# --- 사내 환경 전용 설정 (사외 repo에 노출 금지) ---
*.internal.yaml
*.internal.yml
*.internal.json
config.internal*
requirements.internal.txt

# --- 로컬 작업 도구 산출물 (Claude Code / OMC) ---
# task-PROCESS-002 (2026-05-21): .claude/ 의 worktrees + 사용자 personal override 만 ignore.
# settings.json / hooks/ / skills/ 는 git tracked — process enforcement layer 의 형상관리.
.claude/worktrees/
.claude/settings.local.json
.omc/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# --- Node ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- Build / dist ---
build/
dist/
*.egg
*.whl

# --- IDE ---
.vscode/
.idea/
*.swp
*.swo

# --- OS ---
.DS_Store
Thumbs.db

# --- docker-compose runtime artifacts ---
.open-webui-data/

# --- PoC 산출물 (큰 산출물 — 별도 commit) ---
poc/reports/
poc/targets/snake-game/injected/

# SQLite session DB artifacts (PR #25 task-MVP-004 + PR #35 cleanup)
.macro-logbot-sessions.db
